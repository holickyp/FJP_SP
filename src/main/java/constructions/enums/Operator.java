package constructions.enums;

public enum Operator {
    MULTIPLICATION,
    DIVISION,
    MODULO,
    PLUS,
    MINUS,
    LOWER_EQUAL_THAN,
    GRATER_EQUAL_THAN,
    LOWER_THAN,
    GRATER_THAN,
    EQUALS,
    NOT_EQUALS,
    AND,
    OR
}
