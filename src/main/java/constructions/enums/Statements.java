package constructions.enums;

public enum Statements {
    IF,
    FOR,
    WHILE,
    DO_WHILE,
    REPEAT_UNTIL,
    SWITCH,
    ASSIGNMENT,
    EXPRESSION,
    METHOD_CALL,
    DECLARATION
}
